# ===============================================
# D&D AI Game Frontend - Production Environment
# ===============================================
# IMPORTANT: Copy this file to .env.production
# These variables are baked into the build at compile time

# API Base URL (via Apache proxy)
VITE_API_URL=https://dnd.scorvan.it/api

# WebSocket URL (via Apache proxy for real-time features)
VITE_WS_URL=wss://dnd.scorvan.it

# ===============================================
# NOTES
# ===============================================
# - These values are embedded into the JavaScript bundle during build
# - Changes require rebuild: docker-compose -f docker-compose.prod.yml build frontend
# - API requests will go through Apache reverse proxy to backend on port 3100
# - WebSocket connections are proxied through Apache with ws:// upgrade
